<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/formStyles.css">
    <title>Edit Journal Entry</title>
</head>
<body>
<div class="container">
    <h1>Edit Journal Entry</h1>
    <form action="/records/<%= journalEntry._id %>?_method=PUT" method="POST">
        <div class="form-group">
            <label for="album_title">Album Title:</label>
            <input type="text" name="album_title" value="<%= journalEntry.album_title %>" required>
        </div>
        <div class="form-group">
            <label for="artist">Artist:</label>
            <input type="text" name="artist" value="<%= journalEntry.artist %>" required>
        </div>
        <div class="form-group">
            <label for="spotify_embed_link">Spotify Embed Link:</label>
            <input type="text" name="spotify_embed_link" value="<%= journalEntry.spotify_embed_link %>" required>
        </div>
        <div class="form-group">
            <label for="personal_notes">Personal Notes:</label>
            <textarea name="personal_notes"><%= journalEntry.personal_notes %></textarea>
        </div>
        <div class="form-group">
            <label for="rating">Rating (1-5):</label>
            <input type="number" name="rating" min="1" max="5" value="<%= journalEntry.rating %>">
        </div>
        <div class="button-container">
            <button type="submit">Save Changes</button>
        </div>
    </form>
</div>
</body>
</html>