<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/profileStyle.css" />
    <title>User Page</title>
</head>
<body>
    <div class="container">
    <div class="header">
        <h1>Welcome to <%= user.username %>'s Record Shop!</h1>
    </div>
    <div class="boards-title">
        <h2>Boards</h2>
    </div>
    <div class="boards-list">
        <ul>
            <% user.boards.forEach(board => { %>
                <li class="board-item"><a href="/boards/<%= board._id %>"><%= board.board_name %></a></li>
            <% }) %>
        </ul>
    </div>
    <div class="buttons">
        <a href="/boards/add" class="btn">Add New Board</a>
    </div>
    <div>
        <button onclick="window.location.href='/users/logout'">Logout</button>
    </div>
    </div>
</body>
</html>